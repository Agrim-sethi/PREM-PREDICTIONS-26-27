rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections
    // This is suitable for a small private predictions league
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
